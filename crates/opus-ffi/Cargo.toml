[package]
name = "opus-ffi"
version = "2.2.2"
edition = "2024"
description = "FFI bindings to C libopus for cross-validation and benchmarking"
links = "opus_c_ref"
publish = false

[build-dependencies]
cc = "1"
cmake = "0.1"

[lints.clippy]
too-many-arguments = "allow"
